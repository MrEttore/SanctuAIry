export function User() {
    return (
        <main className="flex flex-1 flex-col items-center justify-center bg-gradient-to-r from-teal-950 from-2% to-slate-800 to-95%">
            {/* TODO: Add load, delete models for the user session. */}
            <div className="text-5xl font-medium text-teal-50">
                Account Settings
            </div>
        </main>
    );
}
